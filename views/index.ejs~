<!DOCTYPE html>
<html>
  <head>
    <title>Twitter</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <% login = false %>
  </head>
  <body>
    <a href="/"><%= first %></a>
    <lang><%= last %></lang>
      <% if (!login) {%>
        <div><% include index_not_login %><div>
      <% } %>
  </body>
</html>
